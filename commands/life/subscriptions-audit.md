---
description: Audit recurring costs & subscriptions (US) — find waste, duplicates, forgotten trials, and renewal traps; produce a prioritized savings + cancellation plan with a template
argument-hint: "[list of subscriptions, or paste a statement]"
disable-model-invocation: true
---

Run the **`subscriptions-audit`** skill via the Skill tool on the user's recurring costs: $ARGUMENTS

Follow the skill — inventory every recurring charge, assess each on the four questions, flag
insurance **gaps** as seriously as savings, and produce the prioritized action plan (with
deadlines and estimated annual savings) plus an optional cancellation template.
